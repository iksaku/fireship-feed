{
  "private": true,
  "name": "fireship-feed",
  "version": "1.0.0",
  "description": "A template for kick starting a Cloudflare Workers project",
  "module": "./dist/index.mjs",
  "scripts": {
    "build": "esbuild ./src/index.ts --bundle --sourcemap --outfile=dist/index.mjs --format=esm",
    "dev": "miniflare --watch",
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write '**/*.{js,css,json,md}'"
  },
  "license": "MIT OR Apache-2.0",
  "devDependencies": {
    "@cloudflare/workers-types": "^3.4.0",
    "esbuild": "^0.14.23",
    "miniflare": "^2.3.0",
    "prettier": "^1.19.1"
  },
  "dependencies": {
    "date-fns": "^2.28.0"
  }
}
